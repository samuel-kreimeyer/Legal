# legal-description-api

- Keep this app as a thin HTTP wrapper around `packages/legal-description`.
- If API contracts become shared dependencies for other systems, extract canonical schemas into `schemas/`.
