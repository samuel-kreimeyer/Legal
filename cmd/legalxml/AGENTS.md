# legalxml-cli

- Treat this app as a documented placeholder until a real command implementation exists.
- When implementation starts, keep parsing and rendering logic in `packages/legal-description`.
