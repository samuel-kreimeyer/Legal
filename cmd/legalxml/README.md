# legalxml-cli

Placeholder app for the LandXML-oriented `legalxml` entrypoint referenced by the source repository.

## Status

The source repository did not contain an executable implementation for this command. Only the original note in `README.source.txt` was preserved during migration.

## Next Step

Implement a real CLI here only after the intended behavior, flags, and output contract are defined.
